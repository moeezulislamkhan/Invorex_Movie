# Native setup

Run:

```bash
flutter pub get
flutter run
```

For Android Studio/VS Code, ensure an Android emulator or physical device is available.

If Gradle reports a stale cache, run:

```bash
flutter clean
flutter pub get
flutter run
```
